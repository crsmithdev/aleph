{
  "private": true,
  "scripts": {
    "test": "bun test.ts",
    "validate": "jq . dotclaude/settings.json > /dev/null && jq . construct/skills/skill-rules.json > /dev/null",
    "dashboard": "npm run dev --prefix construct/dashboard",
    "dashboard:build": "npm run build --prefix construct/dashboard",
    "dashboard:test": "npm test --prefix construct/dashboard"
  }
}
