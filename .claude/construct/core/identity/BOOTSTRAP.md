# Bootstrap

Session initialization sequence. Read by session-start.sh on every session start.

1. Load CONTEXT.md — what's active right now
2. Check memory/snapshots/ — any unresolved work
3. Show last 2 LEARNED.md entries — recent insights
4. Report session count
5. If a worktree is active, state which task it belongs to
