
# Sessions list
- swap the left / right axes on the bar chart, put session count on the right, messages on the left
- adjust 'last active', style 'about 11 hours ago' to '11h', widen the column just enough for the header text to fit on one row

# Sessions detail

- put the 'show spans' link to the right of the model / tool / hook info above the message, just leave the caret, remove the text
- Capitalize the model name, convert it like 'sonnet-4-6' -> 'Sonnet 4.6'

# Skills detail
- make the session id in the table a link to the session detail page for that id; scroll to the message associated with that tool call and highlight the message.
- Make the 'request' column take up at least half the total width, give the other minimal space
- is there anything we can put in the expanded row other than the skill name?
- fix the 'NaN%' success rate on the stats cards, example: http://localhost:3000/observability/skills/inline

# skills list
- widen the messages column just enough for the text to fit on one line
- add a 'last use' column to on the left, styled like the sessions list 'last active' column.
- when expanding the display of tool calls, hooks, etc. in an assistant message, make it expand above the message, not below
- Change 'visible context' on the sidebar to 'Context', make it open by default.  Allow selecting a message to display context up to that point, be sure /clear is handled properly.
- make sure skills and literally any other consumer of tokens are represented in the context sidebar
- add a % of context display next to the # of tokens when segmenting by category
- change the 'total tokens' text at the top to 'tokens'
- re: the note at the bottom about estimations, add tracking of that data to telemetry and update whatever else is needed, NOTHING should be esimated unless it's impossible to get because Claude doesn't expose it or anything that can be used to derive it
- there should be buttons on the right side of the main message display, at the top and bottom, to allow jumping to the top or bottom.  If at the top, hide the top button, if at bottom, hide that one.  If the session is very short, don't display them.
- put the display of total tokens, cost per messages and time above the message instead of below, with the date display.  add a running cost total.
- change any italics display back to regular text.
- add tracking of user interrupts if not already present, show those between messages when they happen, highlighted.
- 'Claude' appears on the left, with nothing else, in user messages, remove it unless there's something happening there on the assistant side.

In observability:
- what are the "Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to." in session detail messages, attributed to the user?
- what about the assistant messages in session detail that have no text content, what's going on there and what could be displayed?


