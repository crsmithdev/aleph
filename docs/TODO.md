
# Observability
- when showing data like 'type' in a chart (e.g. donut charts segmented by type for sessions), make sure the text for the series name has a capital first letter; remove dashes, underscores, etc. from displayed names.  Check every chart in observability.
- reduce overall smoothing in area / line charts by ~50%


# Sessions list
- in the session list page table, change the messages column to show total messages, then in parentheses, assitant and user, separated by /, like "total (assitant / user)"
- swap the left / right axes on the bar chart

# Sessions detail

- in session detail, add markdown support. display **bold** text in messages in bold, lists with bullets, etc.
- give assistant messages the same visual styling as user messages.
- remove the turns stat card, add skills used, and put errors (currently under 'cost') in both tool calls and hook runs, like "succeeded / error"
- when expanding the display of tool calls, hooks, etc. in an assitatnt message, make it expand above the message, not below
- Change 'visible context' on the sidebar to 'Context', make it open by default.  Allow selecting a message to display context up to that point, be sure /clear is handled properly.
- make sure skills and literally any other consumer of tokens are represented in the context sidebar
- add a % of context display next to the # of tokens when segmenting by category
- change the 'total tokens' text at the top to 'tokens'
- re: the note at the bottom about estimations, add tracking of that data to telemetry and update whatever else is needed, NOTHING should be esimated unless it's impossible to get because Claude doesn't expose it or anything that can be used to derive it
- there should be buttons on the right side of the main message display, at the top and bottom, to allow jumping to the top or bottom.  If at the top, hide the top button, if at bottom, hide that one.  If the session is very short, don't display them.
- put the display of total tokens, cost per messages and time above the message instead of below, with the date display.  add a running cost total.
- change any italics display back to regular text.
- add tracking of user interrupts if not alreayd presernt, show those between messages when they happen, highlighted.
- 'Claude' appears on the left, with nothing else, in user messages, remove it unless there's something happening there on the assistant side.

- what are the "Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to." messages, attributed to the user?
- what about the assistant messages that have no text content, what's going on there and what could be displayed?


