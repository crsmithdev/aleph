name: CI

on:
  push:
    branches: [main, "feature/**", "fix/**", "refactor/**", "docs/**"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Tests
        run: bun test.ts

  dashboard:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: construct/dashboard
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Settings JSON valid
        run: jq . dotclaude/settings.json > /dev/null

      - name: Skill rules JSON valid
        run: jq . construct/skills/skill-rules.json > /dev/null

      - name: Check hook files exist
        run: |
          jq -r '.. | .command? // empty' dotclaude/settings.json \
            | grep -v ccstatusline \
            | sed 's/^bun //' \
            | while read -r path; do
                if [ ! -f "$path" ]; then
                  echo "FAIL: hook file not found: $path"
                  exit 1
                fi
              done
          echo "All hook files exist"

      - name: Install script syntax check
        run: bun build install.ts --no-bundle > /dev/null
