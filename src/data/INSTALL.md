# @aleph/data — Post-install Checks

All paths relative to `~/.claude/aleph/data/`. Run every check. Do not skip or summarize.

## Files

- `src/client.ts` exists
- `src/index.ts` exists
- `package.json` exists and contains `@aleph/data`
