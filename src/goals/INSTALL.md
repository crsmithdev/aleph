# @construct/goals — Post-install Checks

All paths relative to `~/.claude/construct/goals/`. Run every check. Do not skip or summarize.

## Files

- `src/index.ts` exists
- `src/schema.ts` exists
- `src/ddl.ts` exists
- `src/services/goals.ts` exists
- `src/services/todos.ts` exists
- `mcp/src/index.ts` exists
- `package.json` exists and contains `@construct/goals`

## Data

- Database at `~/.claude/construct/data/construct.db` may or may not exist (created on first use)
