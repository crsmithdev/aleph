Example invocation: "debug this — my auth middleware is returning 401 for valid tokens". The skill walks the four phases (root-cause investigation, pattern analysis, hypothesis + minimal test, fix) and refuses to propose any change until Phase 1 evidence points to a specific root cause. For multi-component failures, it adds diagnostic instrumentation at each boundary first, then runs once to gather evidence before touching any code.
