Add a `unique<T>(arr: T[]): T[]` function to `utils.ts` that returns a new array with duplicate values removed, preserving the order of first occurrence.

Example: `unique([1, 2, 1, 3, 2])` → `[1, 2, 3]`
