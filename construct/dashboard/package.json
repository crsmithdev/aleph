{
  "name": "construct-dashboard",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "mcp",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w shared && npm run build -w api && npm run build -w web",
    "test": "npm test -w api",
    "test:e2e": "npx playwright test",
    "seed": "npx tsx scripts/seed.ts",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
