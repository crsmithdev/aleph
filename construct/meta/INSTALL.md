# construct-meta — Post-install Checks

All paths relative to `~/.claude/`. Run every check. Do not skip or summarize.

## Files

- `construct/meta/README.md` exists
- `commands/construct.md` exists
- `commands/construct.md` contains subcommands: `install`, `verify`, `grasp`, `status`, `retain`, `trace`

## Data

- `~/.claude/construct/.trace` does not exist by default (tracing off)
